<<<FILES
[
  {"path": "tools/zip_output.py", "content": "import os\nimport zipfile\nimport argparse\n\ndef zip_folder(folder_path: str, output_path: str) -> None:\n    \"\"\"Zips the specified folder into a zip file.\"\"\"\n    with zipfile.ZipFile(output_path, 'w', zipfile.ZIP_DEFLATED) as zipf:\n        for root, _, files in os.walk(folder_path):\n            for file in files:\n                file_path = os.path.join(root, file)\n                zipf.write(file_path, os.path.relpath(file_path, folder_path))\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description='Zip a folder.')\n    parser.add_argument('folder', type=str, help='The folder to zip')\n    parser.add_argument('output', type=str, help='The output zip file path')\n    args = parser.parse_args()\n    zip_folder(args.folder, args.output)\n\n\nif __name__ == '__main__':\n    main()\n"},
  {"path": "tests/test_zip_output.py", "content": "import os\nimport zipfile\nimport pytest\nfrom tools.zip_output import zip_folder\n\ndef test_zip_folder(tmp_path):\n    folder_to_zip = tmp_path / 'test_folder'\n    folder_to_zip.mkdir()\n    (folder_to_zip / 'file1.txt').write_text('Hello, World!')\n    (folder_to_zip / 'file2.txt').write_text('Another file.')\n    output_zip = tmp_path / 'output.zip'\n    zip_folder(str(folder_to_zip), str(output_zip))\n    assert output_zip.is_file()\n    with zipfile.ZipFile(output_zip, 'r') as zipf:\n        assert 'file1.txt' in zipf.namelist()\n        assert 'file2.txt' in zipf.namelist()\n"},
  {"path": "skills.json", "content": "{\n  \"skills\": [\n    {\n      \"name\": \"zip_output\",\n      \"description\": \"Zips a specified folder into a zip file.\"\n    }\n  ]\n}"}
]
FILES>>>