<<<FILES
[
  {"path": "bin/uj-health.py", "content": "#!/usr/bin/env python3\nimport sys\nimport os\nimport subprocess\n\n\ndef check_python_version() -> str:\n    \"\"\"Check Python version.\"\"\"\n    version = sys.version_info\n    return f'Python version: {version.major}.{version.minor}.{version.micro}'\n\n\ndef check_venv() -> str:\n    \"\"\"Check if virtual environment is active.\"\"\"\n    return 'Venv is active' if 'VIRTUAL_ENV' in os.environ else 'Venv is not active'\n\n\ndef check_dependencies() -> str:\n    \"\"\"Check if dependencies are installed.\"\"\"\n    try:\n        subprocess.run(['pip', 'check'], check=True)\n        return 'All dependencies are installed'\n    except subprocess.CalledProcessError:\n        return 'Some dependencies are missing'\n\n\ndef check_env_vars() -> str:\n    \"\"\"Check if required environment variables are set.\"\"\"\n    required_vars = ['VAR1', 'VAR2']  # Replace with actual required vars\n    missing_vars = [var for var in required_vars if var not in os.environ]\n    return 'All required env vars are set' if not missing_vars else f'Missing env vars: {", ".join(missing_vars)}'\n\n\ndef check_repo_permissions() -> str:\n    \"\"\"Check if the user has write permissions to the repository.\"\"\"\n    return 'User has write permissions' if os.access(os.getcwd(), os.W_OK) else 'User does not have write permissions'\n\n\ndef main() -> None:\n    checks = [\n        check_python_version(),\n        check_venv(),\n        check_dependencies(),\n        check_env_vars(),\n        check_repo_permissions()\n    ]\n    for check in checks:\n        print(check)\n\n\nif __name__ == '__main__':\n    main()\n"},
  {"path": "pytest.ini", "content": "[pytest]\naddopts = --maxfail=1 --disable-warnings -q\n"},
  {"path": "tests/test_uj_health.py", "content": "import subprocess\nimport os\nimport pytest\n\n\ndef test_check_python_version():\n    result = subprocess.run(['python3', 'bin/uj-health.py'], capture_output=True, text=True)\n    assert 'Python version:' in result.stdout\n\n\ndef test_check_venv():\n    os.environ['VIRTUAL_ENV'] = 'test_venv'\n    result = subprocess.run(['python3', 'bin/uj-health.py'], capture_output=True, text=True)\n    assert 'Venv is active' in result.stdout\n    del os.environ['VIRTUAL_ENV']\n\n\ndef test_check_dependencies():\n    result = subprocess.run(['python3', 'bin/uj-health.py'], capture_output=True, text=True)\n    assert 'All dependencies are installed' in result.stdout or 'Some dependencies are missing' in result.stdout\n\n\ndef test_check_env_vars():\n    os.environ['VAR1'] = 'value'\n    result = subprocess.run(['python3', 'bin/uj-health.py'], capture_output=True, text=True)\n    assert 'All required env vars are set' in result.stdout\n    del os.environ['VAR1']\n\n\ndef test_check_repo_permissions():\n    result = subprocess.run(['python3', 'bin/uj-health.py'], capture_output=True, text=True)\n    assert 'User has write permissions' in result.stdout or 'User does not have write permissions' in result.stdout\n"}
]
FILES>>>